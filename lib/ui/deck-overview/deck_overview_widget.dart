import 'package:aidex/ui/deck-overview/deck_overview_state.dart';
import 'package:flutter/cupertino.dart';

/// This widget is used to display the deck overview.
class DeckOverviewWidget extends StatefulWidget {
  /// Constructor for the [DeckOverviewWidget].
  ///
  /// The [key] is used to identify the widget in the widget tree.
  const DeckOverviewWidget({super.key});

  @override
  DeckOverviewState createState() => DeckOverviewState();
}
